CREATE TABLE IF NOT EXISTS messages (id BIGSERIAL PRIMARY KEY, chat_id BIGINT NOT NULL, sender_id BIGINT, body TEXT NOT NULL, is_encrypted BOOLEAN NOT NULL DEFAULT TRUE, sent_at TIMESTAMPTZ NOT NULL DEFAULT NOW(), is_removed BOOLEAN NOT NULL DEFAULT FALSE);
CREATE INDEX IF NOT EXISTS idx_messages_chat ON messages(chat_id);